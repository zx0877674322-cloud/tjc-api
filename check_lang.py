import pytesseract
import os

# 1. ตั้งค่า Path (ต้องตรงกับที่ลงโปรแกรมไว้)
# ถ้าลงไว้ที่อื่น ให้แก้ path ตรงนี้ครับ
tesseract_path = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
pytesseract.pytesseract.tesseract_cmd = tesseract_path

print(f"กำลังตรวจสอบที่: {tesseract_path} ...\n")

try:
    # 2. สั่งให้ Tesseract รายงานภาษาที่มี
    langs = pytesseract.get_languages()
    
    print("========================================")
    print(f"📂 พบภาษาที่ติดตั้งแล้ว: {len(langs)} ภาษา")
    print("========================================")
    
    found_thai = False
    for lang in langs:
        if lang == 'tha':
            print(f"  ✅ {lang} (ภาษาไทย - พร้อมใช้งาน!)")
            found_thai = True
        elif lang == 'eng':
            print(f"  ✅ {lang} (ภาษาอังกฤษ - พร้อมใช้งาน!)")
        else:
            print(f"  - {lang}")
            
    print("========================================")
    
    if found_thai:
        print("\n🎉 ยอดเยี่ยม! เครื่องคุณมีภาษาไทยแล้ว")
        print("👉 คุณสามารถรัน api.py ได้เลยครับ อ่านชื่อไทวัสดุออกแน่นอน!")
    else:
        print("\n❌ ยังไม่พบภาษาไทย (tha) ครับ")
        print("👉 กรุณาดาวน์โหลดไฟล์ tha.traineddata ไปใส่ในโฟลเดอร์ tessdata ก่อนนะครับ")

except Exception as e:
    print("❌ เกิดข้อผิดพลาด (Error):")
    print(e)
    print("\nข้อแนะนำ: ลองเช็คว่าไฟล์ tesseract.exe อยู่ตรงตาม Path ด้านบนจริงหรือไม่")